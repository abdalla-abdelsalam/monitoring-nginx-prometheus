apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: alertmanager-config
  labels:
    resource: prometheus
spec:
  route:
    groupBy: ['severity']
    groupWait: 30s
    groupInterval: 5m
    repeatInterval: 12h
    receiver: 'email-notifications'
  receivers:
    - name: 'email-notifications'
      emailConfigs:
        - to: 'abdallahmahdyyy@gmail.com'
          from: 'testingfeature101@gmail.com'
          smarthost: 'smtp.gmail.com:587'
          authUsername: 'testingfeature101@gmail.com'
          authIdentity: 'testingfeature101@gmail.com'
          authPassword:
            name: gmail-auth
            key: password
